<?php
// define variables
$number = 25;
// if statement - if number is > than 0
// control the value of logical statement
var_dump(expression.$number > 0);
if($number > 0) {
    // work if number > than 0

    echo $number . ' on positiivne';
    if($number < 0) {
        // work if number < than 0
        echo $number.'on negatiivne<br>';
    }
}
// the program is continue

